# My Wezterm config

Windows-only.

## Installation

Open PowerShell in administrator mode and run:

```Initialize.ps1```

Or just run the script normally, let the symlinks fail, and create them yourself.
